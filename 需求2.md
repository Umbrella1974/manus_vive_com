你现在要修改仓库 manus_vive_com / manus_vivetracker_communication。以当前打开的仓库为准。

目标：
新增 raw JSONL capture 功能，并在 C++ combined JSON 中加入更明确的时间戳字段，用于后续 exp2 判断 MANUS skeleton 和 Vive tracker 是否同步。

重要原则：
Python 端只负责保存收到的 raw combined JSON，不要在 Python 端给 frame_data 补字段。
所有新增时间戳字段都应该在 C++ 端生成 combined JSON 时写进去。
这样 Python 保存的 JSONL 才是真正的 raw combined JSON。

一、C++ combined JSON 时间戳字段

保留现有字段：
- timestamp
  继续表示 combined JSON 生成时的 system_clock Unix epoch ms。
  不要改语义，保持兼容。
- frame
  继续表示 combined frame counter。
  不要改语义，保持兼容。

新增字段：
- combined_monotonic_ms
  combined JSON 生成时的 steady_clock 单调时间 ms。

Skeleton 相关字段：
- skeleton_publish_time
  如果 SkeletonStreamInfo.publishTime 可用，保存 SDK 原始 publishTime 值。
  不要强行解释单位。
  如果不可用，设为 null。
- skeleton_receive_monotonic_ms
  最新 skeleton callback 到达程序时的 steady_clock ms。
- skeleton_frame
  如果 SDK 有真实 skeleton frame id，则保存；否则设为 null。
- skeleton_callback_index
  skeleton callback 自增序号。注意它不是 SDK frame，只是本程序收到 skeleton callback 的计数。

Tracker 相关字段：
- tracker_publish_time
  如果 TrackerStreamInfo.publishTime 可用，保存 SDK 原始 publishTime 值。
  不要强行解释单位。
  如果不可用，设为 null。
- tracker_last_update_time
  如果 TrackerData.lastUpdateTime 可用，保存 SDK 原始值。
  不要强行解释单位。
  如果不可用，设为 null。
- tracker_receive_monotonic_ms
  最新 tracker callback 到达程序时的 steady_clock ms。
- tracker_frame
  如果 SDK 有真实 tracker frame id，则保存；否则设为 null。
- tracker_callback_index
  tracker callback 自增序号。注意它不是 SDK frame，只是本程序收到 tracker callback 的计数。

字段语义：
当前 combined JSON 使用的是 latest skeleton cache + latest tracker cache。
所以 skeleton_* 表示“本 combined frame 中使用的那份 skeleton 缓存最后一次更新的时间/索引”。
tracker_* 表示“本 combined frame 中使用的那份 tracker 缓存最后一次更新的时间/索引”。
不要把它们解释成 combined frame 生成时两路同时更新。

二、C++ 缓存逻辑

在 skeleton callback 中：
- 更新 latest skeleton 数据
- 记录 skeleton_publish_time
- 记录 skeleton_receive_monotonic_ms
- skeleton_callback_index++

在 tracker callback 中：
- 更新 latest tracker 数据
- 记录 tracker_publish_time
- 记录 tracker_last_update_time，如果可用
- 记录 tracker_receive_monotonic_ms
- tracker_callback_index++

在 CombinedToJSON 或生成 combined JSON 的函数中：
- 写入 timestamp / frame
- 写入 combined_monotonic_ms
- 写入最新缓存的 skeleton_* 字段
- 写入最新缓存的 tracker_* 字段
- 写入 skeletons / trackers 数据

三、Python raw JSONL capture

修改 manus_data_receiver.py：

给 ManusDataReceiver 增加：
- raw_jsonl_path: str | None = None
- flush_raw_jsonl: bool = True

如果 raw_jsonl_path 不为 None：
- 打开输出文件
- 每次 _process_frame(frame_data) 收到一帧 raw dict 后，原样写入 JSONL
- 每行 json.dumps(frame_data, ensure_ascii=False)
- 不要给 frame_data 补字段
- 不要改写字段名
- flush_raw_jsonl=True 时每帧 flush
- stop/close 时关闭文件
- raw_jsonl_path 的父目录要自动创建：mkdir(parents=True, exist_ok=True)

四、capture_raw_jsonl.py

新增命令行脚本：
- --host 默认 127.0.0.1
- --port 默认 8888
- --out 默认 data/raw_frames.jsonl
- --duration 可选
- --max-frames 可选
- --flush 默认 True
- --print-every 默认 30

脚本启动 ManusDataReceiver，保存 raw JSONL。
Ctrl+C 时要优雅关闭文件。
结束时打印 total frames、output path、elapsed time。

五、inspect_raw_jsonl.py

新增检查脚本：
- --path raw_frames.jsonl

输出：
- total lines
- valid json lines
- frames with skeletons
- frames with trackers
- frames with node 4
- frames with node 9
- frames with tracker position
- frames with tracker valid=true
- frames with combined_monotonic_ms
- frames with skeleton_publish_time
- frames with tracker_publish_time
- frames with skeleton_receive_monotonic_ms
- frames with tracker_receive_monotonic_ms
- frames with skeleton_callback_index
- frames with tracker_callback_index
- min/mean/max abs(skeleton_receive_monotonic_ms - tracker_receive_monotonic_ms) if available
- min/mean/max combined_monotonic_ms - skeleton_receive_monotonic_ms if available
- min/mean/max combined_monotonic_ms - tracker_receive_monotonic_ms if available

六、不要做

不要做：
- 不要在 Python 端补 skeleton/tracker timestamp 字段
- 不要改 timestamp 的现有语义
- 不要混用 system_clock epoch ms 和 steady_clock monotonic ms
- 不要把 skeleton_callback_index 伪装成 skeleton_frame
- 不要做复杂同步、插值、重采样
- 不要计算 pinch_center
- 不要接 exp2
- 不要做 offset 校准或可视化

完成后请报告：
1. C++ 新增了哪些字段
2. 每个时间字段的时钟源和语义
3. Python raw JSONL capture 如何运行
4. inspect_raw_jsonl.py 如何运行
5. 是否保持旧 timestamp/frame 字段兼容