本需求是对 需求2.md / 需求3.md 的最终修正。
需求2.md 是主体需求，需求3.md 和本文档用于覆盖/修正其中有冲突或不明确的条目。
如果三者有冲突，以本文档为准。

一、combined JSON 发送策略

C++ 端需要改造成 latest cache + event-driven send 模式。

1. Skeleton callback 到达时：
   - 更新 latest_skeleton_cache
   - 更新 skeleton_publish_time
   - 更新 skeleton_receive_monotonic_ms
   - skeleton_callback_index++
   - 标记 skeleton 有新数据
   - 触发生成/发送一帧 combined JSON

2. Tracker callback 到达时：
   - 更新 latest_tracker_cache
   - 更新 tracker_publish_time
   - 更新每个 tracker 的 last_update_time
   - 更新 tracker_receive_monotonic_ms
   - tracker_callback_index++
   - 标记 tracker 有新数据
   - 触发生成/发送一帧 combined JSON

3. combined JSON 内容：
   - 使用 latest_skeleton_cache + latest_tracker_cache
   - 如果某一路从未收到过，则该路数组为空：
       skeletons = []
       或 trackers = []
   - 对应时间字段为 JSON null
   - skeleton_* 字段表示当前 combined JSON 中使用的那份 latest skeleton cache 的更新时间/索引
   - tracker_* 字段表示当前 combined JSON 中使用的那份 latest tracker cache 的更新时间/索引

4. 避免空转重复发送：
   - 没有新的 skeleton callback 或 tracker callback 时，不生成新的 combined JSON
   - 不要在主循环里反复发送完全相同的 latest cache
   - 每一帧 combined JSON 都必须由至少一路新 callback 触发

二、frame 语义

保留顶层字段：
- timestamp
- frame

timestamp：
- 保持旧语义
- combined JSON 生成时的 system_clock Unix epoch ms

frame：
- 改成 / 明确为 combined JSON send counter
- 每成功生成/发送一帧 combined JSON 时递增 1
- 与 skeleton callback index、tracker callback index 分开
- 不要只在 skeleton 更新时递增

如果需要记录 callback 更新次数，使用：
- skeleton_callback_index
- tracker_callback_index

不要把 skeleton_callback_index / tracker_callback_index 伪装成 skeleton_frame / tracker_frame。

三、tracker last update time

TrackerData.lastUpdateTime 是每个 tracker 自己的字段。

因此：
- 不要在顶层输出 tracker_last_update_time
- 不要输出 tracker_last_update_time_max
- 在每个 tracker object 内输出：
    "last_update_time": <TrackerData.lastUpdateTime.time 或 null>

示例：

"trackers": [
  {
    "id": 0,
    "trackerId": "...",
    "position": [...],
    "rotation": [...],
    "quality": ...,
    "valid": true,
    "last_update_time": 123456
  }
]

如果 lastUpdateTime.time == 0，则输出 JSON null，不要输出字符串 "null"。

四、publishTime 序列化

SkeletonStreamInfo.publishTime 和 TrackerStreamInfo.publishTime 是 ManusTimestamp。
序列化时写 publishTime.time 的数值。
如果 publishTime.time == 0，输出 JSON null。

字段：
- skeleton_publish_time
- tracker_publish_time

不要尝试把整个 publishTime 结构体写进 JSON。

五、frame 字段与 skeleton_frame / tracker_frame

如果 SDK 没有真实 skeleton frame id / tracker frame id：
- skeleton_frame = null
- tracker_frame = null

注意：
- null 必须是 JSON literal null
- 不是字符串 "null"

六、monotonic 时间字段

以下字段来自 steady_clock：
- combined_monotonic_ms
- skeleton_receive_monotonic_ms
- tracker_receive_monotonic_ms

这些字段只用于同一次程序运行 / 同一个 capture session 内计算时间差。
不要把它们当 Unix 时间。
不要跨录制文件比较它们的绝对值。
不要和 timestamp 混用成绝对时间。

七、Python raw JSONL capture

Python 端只保存收到的 raw combined JSON。
不要在 Python 端给 frame_data 补 skeleton/tracker 时间戳字段。
新增字段必须由 C++ 端生成。

capture_raw_jsonl.py 的 --duration：
- 从收到第一帧 raw frame 后开始计时
- 不要从脚本启动时开始计时
- 如果一直没有收到帧，不应因为 duration 提前退出
- 可选增加 --startup-timeout，但第一版不是必须

输出路径：
- 如果 --out 是 data/raw_frames.jsonl，需要自动创建 data/ 目录
- 使用 Path(out).parent.mkdir(parents=True, exist_ok=True)

八、inspect_raw_jsonl.py

inspect 脚本需要统计：
- total lines
- valid json lines
- frames with skeletons
- frames with trackers
- frames with node 4
- frames with node 9
- frames with tracker position
- frames with tracker valid=true
- frames with combined_monotonic_ms
- frames with skeleton_publish_time
- frames with tracker_publish_time
- frames with skeleton_receive_monotonic_ms
- frames with tracker_receive_monotonic_ms
- frames with skeleton_callback_index
- frames with tracker_callback_index
- frames where each tracker has last_update_time

如果字段存在，统计：
- abs(skeleton_receive_monotonic_ms - tracker_receive_monotonic_ms)
- combined_monotonic_ms - skeleton_receive_monotonic_ms
- combined_monotonic_ms - tracker_receive_monotonic_ms

输出 min / mean / max。
字段为 null 时跳过。

九、不要做

不要做：
- 不要在没有新 callback 时重复发送同一份 combined JSON
- 不要把 tracker lastUpdateTime 放成顶层单字段
- 不要伪造 skeleton_frame / tracker_frame
- 不要把 JSON null 写成字符串 "null"
- 不要在 Python 端补 C++ 时间戳字段
- 不要做复杂同步、插值、重采样
- 不要计算 pinch_center
- 不要接 exp2