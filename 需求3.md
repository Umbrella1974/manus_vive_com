同意你的判断，尤其是 latest cache 和 tracker_last_update_time 两点需要修正。

我会把需求改成：
1. C++ 端需要改造成 latest cache 模式，而不是假设当前已经是 latest cache。
2. Python 端只原样保存 C++ 生成的 raw combined JSON，不补字段。
3. timestamp/frame 保持旧语义。
4. 新增 combined_monotonic_ms、skeleton_receive_monotonic_ms、tracker_receive_monotonic_ms。
5. SDK publishTime 保存 publishTime.time；time==0 时输出 JSON null。
6. TrackerData.lastUpdateTime 放到每个 tracker 对象的 last_update_time 字段里，顶层 tracker_last_update_time_max 可选。
7. skeleton_frame/tracker_frame 没有真实 SDK frame 时输出 JSON null；callback index 另用 skeleton_callback_index/tracker_callback_index。
8. monotonic 字段只用于同一 capture session 内计算 delta，不能当绝对 Unix 时间。
9. capture_raw_jsonl.py 的 --duration 从收到第一帧后开始计时。
10. data/ 目录自动创建。

不要自由扩展需求。
不要重构无关代码。
不要引入新架构。
先解释你理解的最小改动范围。
然后给出文件级修改计划。
等我确认后再写代码。